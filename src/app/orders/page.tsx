import BottomBar from "@/components/BottomBar";
import React from "react";

type Props = object;

function page({}: Props) {
  return (
    <div>
      <BottomBar />
    </div>
  );
}

export default page;
